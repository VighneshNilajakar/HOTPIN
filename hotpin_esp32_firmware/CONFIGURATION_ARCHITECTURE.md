# 🎨 Configuration System Architecture

## 📊 Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    USER EDITS                               │
└─────────────────────────────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │         .env File                    │
        │  HOTPIN_SERVER_IP=10.240.253.58     │
        │  HOTPIN_SERVER_PORT=8000            │
        │  HOTPIN_WIFI_SSID=SGF14             │
        │  HOTPIN_WIFI_PASSWORD=12345678vn    │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │  scripts/apply_env_config.py         │
        │  - Reads .env                        │
        │  - Parses KEY=VALUE                  │
        │  - Updates sdkconfig                 │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │         sdkconfig File               │
        │  CONFIG_HOTPIN_SERVER_IP=...         │
        │  CONFIG_HOTPIN_SERVER_PORT=...       │
        │  CONFIG_HOTPIN_WIFI_SSID=...         │
        │  CONFIG_HOTPIN_WIFI_PASSWORD=...     │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │     ESP-IDF Kconfig System           │
        │  - Processes sdkconfig               │
        │  - Generates C defines               │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │      main/include/config.h           │
        │  #define CONFIG_WEBSOCKET_URI        │
        │    "ws://" CONFIG_HOTPIN_SERVER_IP   │
        │  #define CONFIG_HTTP_SERVER_URL      │
        │    "http://" CONFIG_HOTPIN_SERVER_IP │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │         C Compiler                   │
        │  - Expands macros                    │
        │  - Generates machine code            │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │      Compiled Firmware (.bin)        │
        │  Contains final configuration        │
        └──────────────────────────────────────┘
                            │
                            ↓
        ┌──────────────────────────────────────┐
        │         ESP32-CAM Device             │
        │  Connects to configured server       │
        └──────────────────────────────────────┘
```

---

## 🔄 Configuration Methods Comparison

```
┌──────────────────────────────────────────────────────────────┐
│                    Method 1: .env File                        │
└──────────────────────────────────────────────────────────────┘

USER:
  1. Edit .env ─────────────────────────┐
                                        │
                                        ↓
  2. python scripts/apply_env_config.py │
                                        │
                                        ↓
  3. idf.py build flash ────────────────┤
                                        │
  ✅ DONE!                              │
  ⏱️  Time: ~1 minute                    │
                                        │
                                        
┌──────────────────────────────────────────────────────────────┐
│                 Method 2: menuconfig                         │
└──────────────────────────────────────────────────────────────┘

USER:
  1. idf.py menuconfig ─────────────────┐
                                        │
  2. Navigate to "HotPin Network        │
     Configuration" menu                │
                                        ↓
  3. Edit values with arrow keys        │
                                        │
  4. Save and exit (S → Q)              │
                                        ↓
  5. idf.py build flash ────────────────┤
                                        │
  ✅ DONE!                              │
  ⏱️  Time: ~2 minutes                   │


┌──────────────────────────────────────────────────────────────┐
│            Method 3: Direct sdkconfig Edit                   │
└──────────────────────────────────────────────────────────────┘

USER:
  1. Open sdkconfig in text editor ─────┐
                                        │
  2. Find CONFIG_HOTPIN_* lines         │
                                        ↓
  3. Manually edit values               │
                                        │
  4. Save file                          │
                                        ↓
  5. idf.py build flash ────────────────┤
                                        │
  ✅ DONE!                              │
  ⏱️  Time: ~3 minutes                   │
  ⚠️  Error-prone                        │
```

---

## 🏗️ File Structure

```
hotpin_esp32_firmware/
│
├── 📄 .env                    ← YOUR CONFIG (git-ignored)
│   └── Edit this to change settings
│
├── 📄 .env.example            ← TEMPLATE (committed to git)
│   └── Copy to .env and edit
│
├── 📄 sdkconfig               ← AUTO-GENERATED (git-ignored)
│   └── Don't edit directly (use script or menuconfig)
│
├── 📁 main/
│   ├── 📄 Kconfig.projbuild  ← MENUCONFIG DEFINITIONS
│   │   └── Defines configuration options
│   │
│   └── 📁 include/
│       └── 📄 config.h        ← C HEADER (uses Kconfig vars)
│           └── Macros expand to final values
│
├── 📁 scripts/
│   └── 📄 apply_env_config.py ← AUTOMATION SCRIPT
│       └── Converts .env → sdkconfig
│
└── 📁 build/
    └── 📄 firmware.bin        ← COMPILED OUTPUT
        └── Contains baked-in config
```

---

## 🔗 Variable Mapping

```
┌────────────────────────────────────────────────────────────┐
│  .env                 sdkconfig              config.h       │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  HOTPIN_SERVER_IP ────→ CONFIG_HOTPIN_    ────→ Used in    │
│                          SERVER_IP              macros      │
│                                                             │
│  HOTPIN_SERVER_PORT ──→ CONFIG_HOTPIN_    ────→ Combined   │
│                          SERVER_PORT            to form     │
│                                                             │
│                                            CONFIG_WEBSOCKET_│
│                                            URI              │
│                                            │                │
│                                            └─ Expands to:   │
│                                               "ws://10.240. │
│                                                253.58:8000/ │
│                                                ws"          │
│                                                             │
│                                            CONFIG_HTTP_     │
│                                            SERVER_URL       │
│                                            │                │
│                                            └─ Expands to:   │
│                                               "http://10.   │
│                                                240.253.58:  │
│                                                8000"        │
└────────────────────────────────────────────────────────────┘
```

---

## 🎯 Automatic Synchronization

```
          SINGLE SOURCE OF TRUTH
                  │
                  ↓
         .env: HOTPIN_SERVER_IP
                  │
          ┌───────┴───────┐
          │               │
          ↓               ↓
    WebSocket URI    HTTP Server URL
          │               │
    "ws://IP:PORT    "http://IP:PORT"
          │               │
          └───────┬───────┘
                  │
                  ↓
          ALWAYS IN SYNC! ✅
      (No manual coordination needed)
```

---

## 🛡️ Security Model

```
┌─────────────────────────────────────────────────────────┐
│                  PUBLIC (Git Repository)                │
├─────────────────────────────────────────────────────────┤
│  ✅ .env.example         (template with defaults)       │
│  ✅ Kconfig.projbuild    (configuration schema)         │
│  ✅ config.h             (no sensitive data)            │
│  ✅ Documentation        (how to configure)             │
└─────────────────────────────────────────────────────────┘
                            │
                            │
                            │ .gitignore blocks
                            │
                            ↓
┌─────────────────────────────────────────────────────────┐
│                PRIVATE (Local Developer)                │
├─────────────────────────────────────────────────────────┤
│  🔒 .env                 (your WiFi password)           │
│  🔒 sdkconfig            (generated config)             │
│  🔒 build/               (compiled firmware)            │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Development Workflow

```
Developer A                Developer B                Server
────────────                ────────────               ──────

1. Clone repo          1. Clone repo
   │                      │
   ↓                      ↓
2. cp .env.example    2. cp .env.example
   to .env                to .env
   │                      │
   ↓                      ↓
3. Edit .env:         3. Edit .env:
   IP=10.240.253.58      IP=10.240.253.58
   SSID=SGF14            SSID=HomeWiFi      ← Different!
   │                      │
   ↓                      ↓
4. apply_env_config   4. apply_env_config
   │                      │
   ↓                      ↓
5. Build & Flash      5. Build & Flash
   │                      │
   │                      │
   └──────┬───────────────┘
          │
          ↓
    Both connect to same server
    (10.240.253.58:8000)
    
    But use different WiFi
    networks! ✅
```

---

## 📊 Before vs After

### BEFORE: Hard-coded Configuration

```
Developer needs to change server IP:

config.h (Line 143):
#define CONFIG_WEBSOCKET_URI "ws://10.240.253.58:8000/ws"
                                  ↑↑↑↑↑↑↑↑↑↑↑↑↑↑
                                  Must change here

config.h (Line 167):
#define CONFIG_HTTP_SERVER_URL "http://10.240.253.58:8000"
                                       ↑↑↑↑↑↑↑↑↑↑↑↑↑↑
                                       AND here (must match!)

❌ Problems:
   - Easy to forget one
   - Creates IP mismatch bugs
   - Requires C code editing
   - Merge conflicts in team
```

### AFTER: Environment-based Configuration

```
Developer needs to change server IP:

.env (One line):
HOTPIN_SERVER_IP=10.240.253.58
                 ↑↑↑↑↑↑↑↑↑↑↑↑↑↑
                 Change ONCE here

✅ Automatically updates:
   - WebSocket URI
   - HTTP Server URL
   - Always in sync
   - No C code editing
   - No merge conflicts
```

---

## 🎓 Learning Curve

```
         Complexity
            ↑
            │
       High │                    ┌──────┐
            │                    │Direct│
            │                    │Edit  │
            │            ┌───────┤sdkconfig
            │            │menu   └──────┘
       Med  │            │config │
            │        ┌───┴───┐   │
            │        │       │   │
            │        └───────┘   │
       Low  │    ┌───────┐       │
            │    │ .env  │       │
            │    │ file  │       │
            └────┴───────┴───────┴────────→
                Easy   Medium   Hard    Time
```

**Recommendation:** Start with `.env` file method! 🌟

---

## 🚀 Quick Commands Cheat Sheet

```bash
# ============================================
# CONFIGURATION
# ============================================

# Edit configuration
nano .env

# Apply changes
python scripts/apply_env_config.py

# Build firmware
idf.py build

# Flash to device
idf.py -p COM5 flash

# Monitor serial output
idf.py -p COM5 monitor

# ============================================
# ONE-LINERS
# ============================================

# Change server IP and deploy
echo "HOTPIN_SERVER_IP=192.168.1.100" >> .env && \
python scripts/apply_env_config.py && \
idf.py build flash

# ============================================
# VERIFICATION
# ============================================

# Check current configuration
grep CONFIG_HOTPIN sdkconfig

# View generated URIs in serial monitor
idf.py monitor | grep -E "Server URI|HTTP client"
```

---

For detailed guides, see:
- 📖 [QUICK_CONFIG_REFERENCE.md](QUICK_CONFIG_REFERENCE.md) - One-page reference
- 📚 [CONFIGURATION_GUIDE.md](CONFIGURATION_GUIDE.md) - Complete guide
- ✅ [CONFIGURATION_IMPLEMENTATION.md](CONFIGURATION_IMPLEMENTATION.md) - Implementation details
