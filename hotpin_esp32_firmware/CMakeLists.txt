# HotPin ESP32-CAM AI Agent Firmware
# Root-level CMakeLists.txt for ESP-IDF build system

cmake_minimum_required(VERSION 3.16)

# Include ESP-IDF build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

# Define the project
project(hotpin_esp32_firmware)
